package com.gmail.ozerianskyy;

import java.util.Arrays;

import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		int[] a = new int[7];
		int i = 0;
		Scanner in = new Scanner(System.in);

		for (int element : a) {

			System.out.print("¬ведите " + (i + 1) + "-й элемент массива: ");
			a[i] = in.nextInt();

			System.out.println(a[i] + " - ");

			i++;
		}

		for (int element : a)

			System.out.print(element + " ");

		// if(sequence[3]-sequence[2]==sequence[1]) {
		// case1;
		// } else if(sequence[3]/sequence[2]==sequence[1]) {
		// case2;
		// } else if(sequence[3]%4==0&&sequence[3]/4%4==0) {
		// case3;
		// }
		//
		//
		// static int case1(sequence[]) {
		//
		//
	}

}
